version: '3'

tasks:
  build:
    desc: Build the standalone pme binary.
    cmds:
      - GOCACHE="${TMPDIR:-/tmp}/pme-go-cache" go build -o pme ./cmd/pme

  test:
    desc: Run the Go test suite.
    cmds:
      - GOCACHE="${TMPDIR:-/tmp}/pme-go-cache" go test ./...
