# A record literal at the top level of a control-flow header is read as a block
# and rejected. Wrap it in parentheses to force a record:
#   if (p == { x: 1 }) { ... }

if { x: 1 } == { x: 1 } { print("equal") }
