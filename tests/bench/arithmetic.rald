# Deterministic integer arithmetic workload.
def checksum(n: int) -> int {
    total = 0
    i = 0
    while i < n {
        total = total + (i % 97)
        i = i + 1
    }
    return total
}

print(checksum(1000))
